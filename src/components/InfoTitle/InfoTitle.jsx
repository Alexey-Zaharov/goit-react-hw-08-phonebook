import css from './InfoTitle.module.css';

const InfoTitle = () => {
  return (
    <p className={css.infoTitle}>
      You have to Log in. Or to make your first registration.
    </p>
  );
};

export default InfoTitle;
